package com.zhiguan.app.ui.theme

import androidx.compose.ui.graphics.Color

// Brand & Accent — the single interactive color
val PrimaryBlue = Color(0xFF0066CC)
val PrimaryFocus = Color(0xFF0071E3)
val PrimaryOnDark = Color(0xFF2997FF)

// Ink — text colors
val Ink = Color(0xFF1D1D1F)
val BodyOnDark = Color(0xFFFFFFFF)
val BodyMuted = Color(0xFFCCCCCC)
val InkMuted80 = Color(0xFF333333)
val InkMuted48 = Color(0xFF7A7A7A)

// Hairlines & Borders
val DividerSoft = Color(0xFFF0F0F0)
val Hairline = Color(0xFFE0E0E0)
val HairlineSoft = Color(0x14000000) // rgba(0,0,0,0.08)

// Canvas — surface colors
val CanvasWhite = Color(0xFFFFFFFF)
val CanvasParchment = Color(0xFFF5F5F7)
val SurfacePearl = Color(0xFFFAFAFC)

// Surface Tiles — dark surfaces
val SurfaceTile1 = Color(0xFF272729)
val SurfaceTile2 = Color(0xFF2A2A2C)
val SurfaceTile3 = Color(0xFF252527)
val SurfaceBlack = Color(0xFF000000)

// Translucent Chip
val SurfaceChipTranslucent = Color(0xA3D2D2D7) // ~64% alpha

// Status Colors — used sparingly, never as primary accent
val StatusGreen = Color(0xFF34C759)
val StatusYellow = Color(0xFFFFCC00)
val StatusGray = Color(0xFFD1D1D6)

// System Red — reserved for destructive actions and error states only
val SystemRed = Color(0xFFE53935)
